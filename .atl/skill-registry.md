# Skill Registry — opencode-bg-subagents

Auto-generated by `sdd-init` on 2026-04-23. Sub-agents MUST check this before starting any task.

## User-Level Skills (relevant subset)

| Skill | Location | Triggers |
|---|---|---|
| `simplify` | `~/.claude/skills/simplify/` | After writing code, review for reuse/quality/efficiency |
| `review` | Global `/review` command | PR review workflows |
| `security-review` | Global `/security-review` | Security review of changes |
| `skill-creator` | `~/.claude/skills/skill-creator/` | Creating new skills following Agent Skills spec |
| `init` | Global `/init` command | Initialize CLAUDE.md in a project |
| `fewer-permission-prompts` | `~/.claude/skills/fewer-permission-prompts/` | Scan transcripts for safe Bash/MCP calls, add to allowlist |
| `update-config` | `~/.claude/skills/update-config/` | Configure Claude Code settings.json (hooks, permissions) |

## SDD Workflow Skills (invoked by `/sdd-*` meta-commands)

| Skill | When |
|---|---|
| `sdd-init` | Bootstrap SDD context in a project (done, this run) |
| `sdd-explore` | Investigate before committing to a change |
| `sdd-propose` | Write change proposal |
| `sdd-spec` | Write delta specs (requirements + scenarios) |
| `sdd-design` | Technical design |
| `sdd-tasks` | Breakdown into checklist |
| `sdd-apply` | Implement tasks |
| `sdd-verify` | Validate impl against specs |
| `sdd-archive` | Close change, merge deltas |

## Project Conventions (read these too)

- **`openspec/project.md`** — project stack, architecture patterns, critical context
- **`openspec/config.yaml`** — per-phase rules for SDD
- **Repo root `README.md`** — general project intro
- **`docs/architecture.md`** — plugin architecture
- **`docs/policy-schema.md`** — policy JSONC schema reference
- **`docs/release-process.md`** — how releases work (CI + OIDC)
- **No AGENTS.md, CLAUDE.md, or similar** at repo root (only user global `~/.claude/CLAUDE.md`)

## Compact Rules (inject into sub-agent prompts as Project Standards)

**Commit format**: Conventional commits (`fix:`, `feat:`, `chore:`, `docs:`, `refactor:`). Branches: `fix/`, `feat/`, `chore/`. NEVER add "Co-Authored-By" or AI attribution.

**Package discipline**: Zero runtime deps in core (pure domain). Protocol package is zero-dep contract. Adapter packages (opencode) depend on core + protocol via `workspace:*` (transformed at publish).

**Testing**: TDD enabled. Run `pnpm -r run test` — 432 tests must stay green. Vitest 2.1.8.

**Build**: `pnpm -r run build` builds all packages via tsc. Never build after changes (per user rule).

**Releases**: Changesets-driven. `changeset publish` via CI with OIDC Trusted Publishing (no NPM_TOKEN). Workspace `workspace:*` deps transformed pre-publish via `scripts/transform-workspace-deps.mjs`.

**Versioning**: Patch bumps for bug fixes, minor for features, major for breaking. Currently: protocol v1.0.0, core v0.1.1, opencode v0.1.4.

**Shell**: User is on Windows 11 PowerShell 5.1 + bash (via Git Bash). Prefer forward-slash paths. Use bash syntax in scripts/examples.

**Host API compat**: Plugin must support BOTH legacy OpenCode API (~1.10) AND OpenCode 1.14+. Runtime detection required.

## How to Load

Before starting any task:

1. Try engram: `mem_search(query: "skill-registry", project: "opencode-bg-subagents")` → get full content via `mem_get_observation`
2. Fallback: read this file directly
3. Match relevant skills by task context
4. Read the specific `SKILL.md` files you need
5. Also read `openspec/project.md` for project-specific context
